{
  "name": "prompt-tree",
  "version": "0.8.0",
  "private": true,
  "description": "Prompt Tree - a personal-use browser extension that augments claude.ai with branch composing, a conversation tree panel, inline notes, margin comments, and draft autosave.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  }
}
